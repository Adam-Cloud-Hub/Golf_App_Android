<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Golf_App.Views.Home"
             BackgroundColor="Gray"
             Shell.NavBarIsVisible="False">

    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <StackLayout Grid.Row="0" VerticalOptions="Center" HorizontalOptions="Center">
                <Image Source="logo.png" Margin="0,10,0,0"/>
            </StackLayout>

            <StackLayout Grid.Row="1" VerticalOptions="Center" HorizontalOptions="Center" >
                <Button Text="New Game" Clicked="bt_New_Game_Click" Style="{StaticResource WhiteButtonStyle}"/>
                <Button x:Name="bt_Continue_Game" Text="Continue Game" Clicked="bt_Continue_Game_Click" Style="{StaticResource WhiteButtonStyle}" Margin="0,20,0,0"/>
            </StackLayout>           
            
        </Grid>                  
    </ContentPage.Content>    
</ContentPage>